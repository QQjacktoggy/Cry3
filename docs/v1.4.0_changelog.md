# v1.4.0 變更紀錄

**日期**: 2026-06-25  
**版本**: `_codex_v1.3.18_micro50_data_emergency_sl` → v1.4.0  
**VM HEAD**: `df39aba`  
**部署方式**: VM 直接修改（未 git commit，hot-patch）

---

## P0: STUP-S 進場 3bp → 1bp

**檔案**: `src/gridbot/strategy/codex_v1_live.py` L1326

```diff
-        entry_offset_bp=3.0,
+        entry_offset_bp=1.0,
```

**原因**: 24h Live 數據 STUP-S 過期率 51%（29/57），Replay 報告證實 1bp 讓 8/17 過期 run 變 TP。Entry shadow 勝率 72.7% 支持策略選股有效，問題在 maker 成交距離。

**預期**: Fill rate 49% → 65-70%

---

## P1: TP Policy Live Override 開開啟

**檔案**: `config/settings.py` L343

```diff
-    mainnet_codex_tp_policy_live_override_enabled: bool = False
+    mainnet_codex_tp_policy_live_override_enabled: bool = True
```

**原因**: TP policy shadow 中 `profit_b_runner45`（tp1=30%, full=25%, runner=45%）擊敗 baseline +0.46bp，MFE capture 40.3% → 42.7%，`primary_promotion_eligible=true`。

**預期**: MFE capture +2.4pp

---

## P3: W6A 風險門檻收緊 ≥4 → ≥3

**檔案**: `src/gridbot/mainnet/one_run.py` L1116-1118

```diff
-                elif risk_score >= 4:
-                    skip_reason = "v137_w6a_risk_score_block"
-                    regime = "v137_w6a_risk_score_4plus"
+                elif risk_score >= 3:
+                    skip_reason = "v137_w6a_risk_score_block"
+                    regime = "v137_w6a_risk_score_3plus"
```

**原因**: W6A 8 runs 中 4 敗（WR 42.9%），淨虧 -0.1826。risk_score=3 原本可 keep50 進場，現直接 block。

**副作用**: else 分支內 `risk_score == 3` 的 keep50/shadow_only 路徑成為 dead code，不影響功能。

---

## P4: signal_timeout 60min → 30min

**檔案**: `config/settings.py` L256

```diff
-    mainnet_one_run_signal_timeout_minutes: int = 60
+    mainnet_one_run_signal_timeout_minutes: int = 30
```

**原因**: 多筆 signal_timeout 佔用 60 分鐘等不到信號，浪費 loop slot。

**預期**: Loop 週轉加速

---

## 排除項目

| 項目 | 原因 |
|---|---|
| API -1003 retry | 其他專案回測卡住，非 bot 問題 |
| Notional 50U | 用戶嚴格指定不動 |
| 0bp 進場 | 先 shadow/canary |
| TTL 45s 延長 | Replay 報告說 TTL 不是瓶頸 |

---

## 備份

- `src/gridbot/strategy/codex_v1_live.py.bak_v1319`
- `src/gridbot/mainnet/one_run.py.bak_v1319`
- `config/settings.py.bak_v1319`

## 驗證

- Syntax check: 3 個檔案 `ast.parse` 全部 OK
- Bot 重啟: PID 2394352，`app_initialized` + `telegram_app_configured` 確認
- 測試: `test_dca_allowed_by_guard_places_order` 等失敗為 pre-existing（git stash 前後皆失敗）